VITE_GTAG_SCRIPT=
VITE_LOGIN_API_HOST="http://localhost"
VITE_RATE_API_HOST="http://localhost"
# 可选：Tauri 桌面 OAuth 本地回环（tauri-plugin-oauth）优先使用的端口；不设则随机可用端口。
# 须在 LOGIN_API 与 GitHub/Gitee OAuth 应用中将 http://127.0.0.1:<端口>/ 登记为允许的 redirect_uri。
# VITE_OAUTH_LOOPBACK_PORT=57891